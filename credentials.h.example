#pragma once

// WiFi credentials — DO NOT COMMIT credentials.h (it is gitignored)
// Copy this file to credentials.h and fill in your actual values:
//   cp credentials.h.example credentials.h
#define WIFI_SSID      "your-wifi-ssid"
#define WIFI_PASSWORD  "your-wifi-password"

// City/location for weather (leave empty for auto-detect by IP, or set e.g. "Delhi")
#define WEATHER_LOCATION  ""
